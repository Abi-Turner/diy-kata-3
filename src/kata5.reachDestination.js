const reachDestination = (distance, speed) => {
 // function calculateTime(distance, speed) {
    //return distance / speed;
  //};


};

//distance / speed and then round up to nearest 0.5 //
//then insert that into a string using template literal//
//string should say 'I should be there in $reachDestination//

module.exports = reachDestination;
